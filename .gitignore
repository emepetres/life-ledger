# Runtime state: SQLite DB file and its WAL sidecars (ADR 0003). Created on first run.
/data/

# Compiled binaries
/life-ledger
/life-ledger.exe
/bin/

# Go test/coverage artifacts
*.out

# First-deploy config: real values live here, never committed. The committed
# .env.example is the template (scripts/first-deploy.ps1, ADR-0005).
/.env

# lockprobe prototype build output (#96) — binaries, never committed
prototypes/lockprobe/dist/
